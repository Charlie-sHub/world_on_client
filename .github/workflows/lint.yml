name: Lint
on:
  push:
    branches:
      - master
      - develop
  pull_request:
    branches:
      - master
      - develop

jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v1
        with:
          java-version: "12.x"
      - uses: subosito/flutter-action@v1
        with:
          channel: "stable"
      - name: Clean
        run: flutter clean
      - name: Get dependencies
        run: flutter pub get
      - name: Generate
        run: flutter pub run build_runner build --delete-conflicting-outputs
      - name: Lint
        run: flutter analyze --no-pub
      # - name: Test (Ignored for now as most are broken)
      #   run: flutter test