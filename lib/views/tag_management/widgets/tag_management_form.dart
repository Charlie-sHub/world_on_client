import 'package:flutter/material.dart';

class TagManagementForm extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: make the tag creation edition form
    return Center(
      child: Text("Tag form"),
    );
  }
}
